# needs-management-need-expression

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build needs-management-need-expression` to build the library.

## Running unit tests

Run `nx test needs-management-need-expression` to execute the unit tests via [Jest](https://jestjs.io).
